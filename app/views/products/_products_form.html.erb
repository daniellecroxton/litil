<%= form_for([business, product]) do |f| %>

<%= form_errors_for product %>


    <%= f.label :name %>
    <%= f.text_field :name, required: true %><br>

    <!-- add image? -->

    Add Tags:<br />
      <%= f.collection_check_boxes :tag_ids, Tag.all, :id, :name %><br />
        <%= f.text_field :new_tag_name %>
      <br />


    <%= f.submit %>

  <% end %>
